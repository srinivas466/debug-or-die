//Question:
The following program tries to print the value of x, but it contains an error. 

#include <stdio.h>

int main() {
    x = 10; // Error: x is not declared
    printf("Value of x: %d", x);
    return 0;
}

//Expected Output
    Value of x: 10
